services:
  enderecamento:
    build: .
    container_name: enderecamento-local
    ports:
      - "8000:8000"
    environment:
      MB_USER: ${MB_USER:-}
      MB_PASS: ${MB_PASS:-}
      MB_SESSION_ID: ${MB_SESSION_ID:-}
    volumes:
      - ./.credentials:/app/.credentials
    restart: unless-stopped
